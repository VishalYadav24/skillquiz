import { CircularProgress } from '@mui/material'
import React, { Fragment } from 'react'

const Loader = () => {
  return (
    <Fragment>
        <CircularProgress/>
    </Fragment>
  )
}

export default Loader